#pragma once

struct ServerInfo {
	// initialize all values to 0
	// region
	int regionID = 0;
	// current player count
	int currentPlayerCount = 0;
	// max player count
	int maxPlayers = 0;
	// ping
	int ping = 0;
};
